abstract class ThemeConstants {
  // 테마 전환시 사용할 애니메이션 지속 시간
  static const Duration themeChangeDuration = Duration(milliseconds: 200);

  // SharedPreferences에서 사용할 테마 모드 키
  static const String themeModeKey = 'theme_mode';
}
